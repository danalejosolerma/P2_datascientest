version: "3.9"
services:
  api_fraud:
    image: image_fraud:latest
    container_name: api_fraud
    networks:
      - bridge
    environment:
      LOG: 1
  tests_api:
    image: image_tests:latest
    container_name: tests_api
    networks:
      - bridge
    volumes:
      - .:/home/test
    environment:
      LOG: 1
networks:
  bridge:
